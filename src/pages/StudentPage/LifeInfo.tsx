import Student from "@/interfaces/Student";

export default function LifeInfo({ student }: { student: Student }) {
  return (
    <div>
      <div>상담 기록</div>
      <div>-- 삭제예정 --</div>
    </div>
  );
}
